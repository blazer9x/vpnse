version: '2'
services:
  vpnse:
    image: vpnse:latest
    volumes:
      - ./data/etc:/etc/vpnserver
      - ./data/log:/usr/vpnserver/log
    cap_add: [NET_ADMIN]
    network_mode: host
    restart: always
