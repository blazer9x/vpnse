version: '2'
services:
  vpnse:
    image: vpnse:latest
    volumes:
      - ./data/softether/etc:/etc/vpnserver
      - ./data/softether/log:/var/log/vpnserver
    cap_add: [NET_ADMIN]
    network_mode: host
    restart: always
