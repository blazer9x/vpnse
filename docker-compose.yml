version: '3'
services:
  vpnse:
    image: vpnse:latest
    volumes:
      - ./data:/etc/vpnserver:rw
    cap_add: [ALL]
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    restart: unless-stopped
    cpus: 1
    mem_limit: 100M
    user: root
